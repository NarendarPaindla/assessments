# Step 19: Category Module (Admin Only)

🎉 We have completed Authentication and User CRUD.

Now we begin **Authorization**.

Many beginners confuse:

| Authentication | Authorization    |
| -------------- | ---------------- |
| Who are you?   | What can you do? |
| Login          | Permissions      |
| JWT Token      | Role Checking    |

Example:

```text
Customer
   ↓
Can Buy Products

Admin
   ↓
Can Create Categories
Can Delete Categories
Can Manage Products
```

---

# 1. Goal

In this step, we will build:

✅ Category CRUD

✅ Role-Based Access Control (RBAC)

✅ Admin-only APIs

✅ Admin Validation Dependency

✅ Category Schemas

✅ Category CRUD Layer

✅ Category Router

By the end, only Admin users can manage categories.

---

# 2. Theory

## Why RBAC?

Imagine:

```text
Customer
   ↓
DELETE Category
```

That would be dangerous.

We need:

```text
Customer
    ↓
Access Denied

Admin
    ↓
Access Granted
```

---

## Authentication vs Authorization

### Authentication

```text
Login
```

Result:

```text
User Identified
```

---

### Authorization

```text
User Role?
```

Result:

```text
Admin?
```

or

```text
Customer?
```

---

# RBAC Flow

```text
JWT Token
     ↓
Current User
     ↓
Check Role
     ↓
Admin?
     ↓
YES → Continue
NO  → 403 Forbidden
```

---

# 3. Folder Structure

Create new files:

```text
app/
│
├── schemas/
│   └── category.py
│
├── crud/
│   └── category.py
│
├── routers/
│   └── category.py
│
├── core/
│   └── permissions.py
```

Updated structure:

```text
app/
│
├── core/
│   ├── security.py
│   └── permissions.py
│
├── schemas/
│   ├── user.py
│   └── category.py
│
├── crud/
│   ├── user.py
│   └── category.py
│
├── routers/
│   ├── user.py
│   └── category.py
```

---

# 4. Full Code

---

## File: app/core/permissions.py

```python
from fastapi import Depends
from fastapi import HTTPException

from app.dependencies import get_current_user


def require_admin(
    current_user=Depends(
        get_current_user
    )
):

    if current_user.role != "admin":

        raise HTTPException(
            status_code=403,
            detail="Admin access required"
        )

    return current_user
```

---

## File: app/schemas/category.py

```python
from pydantic import BaseModel


class CategoryCreate(BaseModel):
    name: str


class CategoryUpdate(BaseModel):
    name: str


class CategoryResponse(BaseModel):
    id: int
    name: str

    model_config = {
        "from_attributes": True
    }
```

---

## File: app/crud/category.py

```python
from sqlalchemy.orm import Session

from app.models.category import Category


def create_category(
    db: Session,
    name: str
):

    category = Category(
        name=name
    )

    db.add(category)

    db.commit()

    db.refresh(category)

    return category


def get_categories(
    db: Session
):
    return db.query(
        Category
    ).all()


def get_category_by_id(
    db: Session,
    category_id: int
):

    return (
        db.query(Category)
        .filter(
            Category.id == category_id
        )
        .first()
    )


def update_category(
    db: Session,
    category_id: int,
    name: str
):

    category = (
        db.query(Category)
        .filter(
            Category.id == category_id
        )
        .first()
    )

    if not category:
        return None

    category.name = name

    db.commit()

    db.refresh(category)

    return category


def delete_category(
    db: Session,
    category_id: int
):

    category = (
        db.query(Category)
        .filter(
            Category.id == category_id
        )
        .first()
    )

    if not category:
        return False

    db.delete(category)

    db.commit()

    return True
```

---

## File: app/routers/category.py

```python
from fastapi import APIRouter
from fastapi import Depends
from fastapi import HTTPException

from sqlalchemy.orm import Session

from app.dependencies import get_db

from app.core.permissions import (
    require_admin
)

from app.schemas.category import (
    CategoryCreate,
    CategoryUpdate,
    CategoryResponse
)

from app.crud.category import (
    create_category,
    get_categories,
    get_category_by_id,
    update_category,
    delete_category
)

router = APIRouter(
    prefix="/categories",
    tags=["Categories"]
)


@router.post(
    "/",
    response_model=CategoryResponse
)
def create_new_category(
    category: CategoryCreate,
    db: Session = Depends(get_db),
    admin=Depends(require_admin)
):
    return create_category(
        db,
        category.name
    )


@router.get(
    "/",
    response_model=list[CategoryResponse]
)
def list_categories(
    db: Session = Depends(get_db)
):
    return get_categories(db)


@router.get(
    "/{category_id}",
    response_model=CategoryResponse
)
def get_category(
    category_id: int,
    db: Session = Depends(get_db)
):

    category = get_category_by_id(
        db,
        category_id
    )

    if not category:
        raise HTTPException(
            status_code=404,
            detail="Category not found"
        )

    return category


@router.put(
    "/{category_id}",
    response_model=CategoryResponse
)
def update_existing_category(
    category_id: int,
    category: CategoryUpdate,
    db: Session = Depends(get_db),
    admin=Depends(require_admin)
):

    updated = update_category(
        db,
        category_id,
        category.name
    )

    if not updated:
        raise HTTPException(
            status_code=404,
            detail="Category not found"
        )

    return updated


@router.delete(
    "/{category_id}"
)
def remove_category(
    category_id: int,
    db: Session = Depends(get_db),
    admin=Depends(require_admin)
):

    deleted = delete_category(
        db,
        category_id
    )

    if not deleted:
        raise HTTPException(
            status_code=404,
            detail="Category not found"
        )

    return {
        "message":
        "Category deleted successfully"
    }
```

---

## File: app/main.py

Add category router.

```python
from fastapi import FastAPI

from app.routers.user import (
    router as user_router
)

from app.routers.category import (
    router as category_router
)

app = FastAPI(
    title="E-Commerce Backend"
)

app.include_router(user_router)
app.include_router(category_router)


@app.get("/")
def home():
    return {
        "message":
        "Welcome to E-Commerce Backend API"
    }
```

---

# 5. Explanation

## require_admin()

This dependency protects routes.

```python
if current_user.role != "admin":
```

Customer:

```text
customer
```

Result:

```json
{
  "detail":"Admin access required"
}
```

---

Admin:

```text
admin
```

Result:

```text
Access Granted
```

---

## Why Depends(require_admin)?

Example:

```python
admin = Depends(require_admin)
```

Before route executes:

```text
Check User
      ↓
Check Role
      ↓
Admin?
```

---

If not admin:

```text
Stop Request
```

---

# Internal Flow

```text
POST /categories
        ↓
JWT Token
        ↓
Current User
        ↓
require_admin()
        ↓
Role Check
        ↓
Admin?
        ↓
Create Category
```

---

# 6. Run Commands

Start server:

```bash
uvicorn app.main:app --reload
```

---

# 7. Test Commands

## Step 1: Create Admin User

For now, manually update database.

Using SQLite:

```sql
UPDATE users
SET role='admin'
WHERE email='narendar@gmail.com';
```

---

## Step 2: Login Again

```json
{
  "email":"narendar@gmail.com",
  "password":"admin123"
}
```

Copy JWT.

---

## Step 3: Swagger Authorize

Click:

```text
Authorize
```

Paste token.

---

## Create Category

```http
POST /categories
```

Body:

```json
{
  "name":"Electronics"
}
```

Expected:

```json
{
  "id":1,
  "name":"Electronics"
}
```

---

## Get Categories

```http
GET /categories
```

Expected:

```json
[
  {
    "id":1,
    "name":"Electronics"
  }
]
```

---

## Update Category

```http
PUT /categories/1
```

Body:

```json
{
  "name":"Electronics & Gadgets"
}
```

---

## Delete Category

```http
DELETE /categories/1
```

Expected:

```json
{
  "message":
  "Category deleted successfully"
}
```

---

# 8. Expected Output

Admin:

```text
Can Create Category
Can Update Category
Can Delete Category
```

Customer:

```json
{
  "detail":
  "Admin access required"
}
```

Status:

```text
403 Forbidden
```

---

# 9. Common Errors

### Error 1

```text
403 Forbidden
```

User role is customer.

Make user admin.

---

### Error 2

```text
401 Unauthorized
```

JWT missing.

Login again.

---

### Error 3

```text
Category not found
```

Wrong category id.

---

### Error 4

```text
IntegrityError
```

Duplicate category name.

We will handle this later with custom exceptions.

---

# 10. Assignment

### Task 1

Create:

```text
app/core/permissions.py
```

---

### Task 2

Implement:

```python
require_admin()
```

---

### Task 3

Create:

```text
CategoryCreate
CategoryUpdate
CategoryResponse
```

---

### Task 4

Implement Category CRUD.

---

### Task 5

Make yourself admin in database.

---

### Task 6

Create category:

```json
{
  "name":"Electronics"
}
```

---

### Task 7

Update category.

---

### Task 8

Delete category.

---

### Task 9

Answer:

1. Difference between Authentication and Authorization?
2. Why do we need RBAC?
3. Why does `require_admin()` return 403 instead of 401?
4. What does `Depends(require_admin)` do?

---

When you're done, type **NEXT**.

In **Step 20: Product Module**, we will build:

✅ Product CRUD

✅ Product Schemas

✅ Category Relationship

✅ Product Creation

✅ Product Listing

✅ Product Update/Delete

This will be the foundation for image uploads, search, filtering, pagination, cart, and orders.
