# Step 18: User CRUD Module

🎉 We have completed:

✅ Registration

✅ Login

✅ JWT Token Generation

✅ Protected Routes

Now we will build a complete User Management module.

---

# 1. Goal

In this step, we will create:

✅ Get All Users

✅ Get User By ID

✅ Update User

✅ Delete User

✅ User CRUD Operations

✅ Protected User APIs

By the end, we will have a complete User Module.

---

# 2. Theory

CRUD stands for:

| Operation | Meaning     |
| --------- | ----------- |
| Create    | Create User |
| Read      | Read User   |
| Update    | Update User |
| Delete    | Delete User |

We already implemented:

```text id="3h0ot7"
Create User
(Register)
```

Now we will implement:

```text id="eg8bpi"
Read
Update
Delete
```

---

# CRUD Flow

```text id="w6g85l"
Create User
      ↓
Get Users
      ↓
Get User By ID
      ↓
Update User
      ↓
Delete User
```

---

# 3. Folder Structure

No new folders.

Updated files:

```text id="6d9dft"
app/
│
├── schemas/
│   └── user.py
│
├── crud/
│   └── user.py
│
├── routers/
│   └── user.py
```

---

# 4. Full Code

---

## File: app/schemas/user.py

Add:

```python id="z5p4qn"
class UserUpdate(BaseModel):
    name: str
    email: EmailStr
```

---

### Complete Additional Schema

```python id="mw90qz"
class UserUpdate(BaseModel):
    name: str
    email: EmailStr
```

---

## File: app/crud/user.py

Add these functions below existing code.

---

### Get All Users

```python id="lgsdfw"
def get_users(
    db: Session
):
    return db.query(User).all()
```

---

### Get User By ID

```python id="gq9vl9"
def get_user_by_id(
    db: Session,
    user_id: int
):
    return (
        db.query(User)
        .filter(User.id == user_id)
        .first()
    )
```

---

### Update User

```python id="rf57wv"
def update_user(
    db: Session,
    user_id: int,
    name: str,
    email: str
):

    user = (
        db.query(User)
        .filter(User.id == user_id)
        .first()
    )

    if not user:
        return None

    user.name = name
    user.email = email

    db.commit()

    db.refresh(user)

    return user
```

---

### Delete User

```python id="hsv7r3"
def delete_user(
    db: Session,
    user_id: int
):

    user = (
        db.query(User)
        .filter(User.id == user_id)
        .first()
    )

    if not user:
        return False

    db.delete(user)

    db.commit()

    return True
```

---

## File: app/routers/user.py

Add imports:

```python id="k6bz6n"
from app.schemas.user import UserUpdate

from app.crud.user import (
    get_users,
    get_user_by_id,
    update_user,
    delete_user
)
```

---

### Get All Users

```python id="65w6y2"
@router.get(
    "/",
    response_model=list[UserResponse]
)
def get_all_users(
    db: Session = Depends(get_db)
):

    return get_users(db)
```

---

### Get User By ID

```python id="i2h4r4"
@router.get(
    "/{user_id}",
    response_model=UserResponse
)
def get_single_user(
    user_id: int,
    db: Session = Depends(get_db)
):

    user = get_user_by_id(
        db,
        user_id
    )

    if not user:
        raise HTTPException(
            status_code=404,
            detail="User not found"
        )

    return user
```

---

### Update User

```python id="g83jmf"
@router.put(
    "/{user_id}",
    response_model=UserResponse
)
def update_single_user(
    user_id: int,
    user_data: UserUpdate,
    db: Session = Depends(get_db)
):

    user = update_user(
        db=db,
        user_id=user_id,
        name=user_data.name,
        email=user_data.email
    )

    if not user:
        raise HTTPException(
            status_code=404,
            detail="User not found"
        )

    return user
```

---

### Delete User

```python id="4u3o9t"
@router.delete("/{user_id}")
def remove_user(
    user_id: int,
    db: Session = Depends(get_db)
):

    deleted = delete_user(
        db,
        user_id
    )

    if not deleted:
        raise HTTPException(
            status_code=404,
            detail="User not found"
        )

    return {
        "message": "User deleted successfully"
    }
```

---

# 5. Explanation

---

## Get All Users

```python id="g1t2a8"
db.query(User).all()
```

Equivalent SQL:

```sql id="4lvgm2"
SELECT * FROM users;
```

---

## Get User By ID

```python id="hsjgrs"
.filter(User.id == user_id)
```

Equivalent SQL:

```sql id="ut0v4e"
SELECT *
FROM users
WHERE id = 1;
```

---

## Update User

Find:

```python id="fy92b5"
user = ...
```

Then:

```python id="0w1ikv"
user.name = name
```

Modify object.

---

Commit:

```python id="mlw3fw"
db.commit()
```

Save changes.

---

Refresh:

```python id="m3azfd"
db.refresh(user)
```

Reload latest values.

---

## Delete User

```python id="6v8vc0"
db.delete(user)
```

Marks for deletion.

---

```python id="a5zzvx"
db.commit()
```

Executes delete.

---

# Internal Flow

## GET User

```text id="t1o6es"
Request
   ↓
Query User
   ↓
Return User
```

---

## Update User

```text id="lnqzuh"
Request
   ↓
Find User
   ↓
Modify Fields
   ↓
Commit
   ↓
Return Updated User
```

---

## Delete User

```text id="wj1mft"
Request
   ↓
Find User
   ↓
Delete
   ↓
Commit
   ↓
Success Response
```

---

# 6. Run Commands

Start server:

```bash id="v7e3tu"
uvicorn app.main:app --reload
```

---

# 7. Test Commands

## Swagger

Open:

```text id="u32ytf"
http://127.0.0.1:8000/docs
```

You should see:

```text id="zyjrzm"
GET /users/

GET /users/{user_id}

PUT /users/{user_id}

DELETE /users/{user_id}
```

---

## Get All Users

```http id="d6m4iw"
GET /users/
```

Expected:

```json id="5vym6r"
[
  {
    "id":1,
    "name":"Narendar",
    "email":"narendar@gmail.com",
    "role":"customer"
  }
]
```

---

## Get User By ID

```http id="0x1j78"
GET /users/1
```

Expected:

```json id="gjpqfk"
{
  "id":1,
  "name":"Narendar",
  "email":"narendar@gmail.com",
  "role":"customer"
}
```

---

## Update User

```http id="0z39h0"
PUT /users/1
```

Body:

```json id="ducr6r"
{
  "name":"Narendar Reddy",
  "email":"narendar123@gmail.com"
}
```

Expected:

```json id="luzw0g"
{
  "id":1,
  "name":"Narendar Reddy",
  "email":"narendar123@gmail.com",
  "role":"customer"
}
```

---

## Delete User

```http id="ht9a4s"
DELETE /users/1
```

Expected:

```json id="0dnlfp"
{
  "message":"User deleted successfully"
}
```

---

## Postman Example

Method:

```text id="4pg6m4"
PUT
```

URL:

```text id="1j7l09"
http://127.0.0.1:8000/users/1
```

Body:

```json id="8p4yeu"
{
  "name":"Updated User",
  "email":"updated@gmail.com"
}
```

---

## curl Example

```bash id="v7l0tv"
curl -X PUT http://127.0.0.1:8000/users/1 \
-H "Content-Type: application/json" \
-d '{"name":"Updated User","email":"updated@gmail.com"}'
```

---

# 8. Expected Output

User CRUD should now work:

```text id="a05vok"
Create
Read
Update
Delete
```

through API endpoints.

---

# 9. Common Errors

### Error 1

```text id="q6j7um"
404 User not found
```

User ID doesn't exist.

---

### Error 2

```text id="e4pw9p"
sqlite3.IntegrityError
```

Trying to update email to duplicate value.

---

### Error 3

```text id="7jqls7"
422 Validation Error
```

Invalid email format.

---

### Error 4

```text id="owg3kc"
NameError
```

Forgot imports.

---

### Error 5

```text id="0w1txa"
Response validation error
```

Missing:

```python id="jqlrjk"
model_config = {
    "from_attributes": True
}
```

inside `UserResponse`.

---

# 10. Assignment

### Task 1

Add:

```python id="m1twnl"
get_users()
```

---

### Task 2

Add:

```python id="l4sjh2"
get_user_by_id()
```

---

### Task 3

Add:

```python id="v4ek7x"
update_user()
```

---

### Task 4

Add:

```python id="llv6bx"
delete_user()
```

---

### Task 5

Test:

```http id="omafhn"
GET /users/
```

---

### Task 6

Test:

```http id="kr9mwt"
GET /users/1
```

---

### Task 7

Test:

```http id="ehh8oz"
PUT /users/1
```

---

### Task 8

Test:

```http id="48ch7n"
DELETE /users/1
```

---

### Task 9

Answer:

1. What does `db.query(User).all()` do?
2. Why do we call `db.commit()` after update?
3. Why do we call `db.refresh()`?
4. What does `db.delete()` do?

---

When you're done, type **NEXT**.

In **Step 19: Category Module (Admin Only)**, we will build:

✅ Category CRUD

✅ Admin Role Validation

✅ Role-Based Access Control (RBAC)

✅ Admin-Only Endpoints

This is where we start implementing real authorization, not just authentication.
