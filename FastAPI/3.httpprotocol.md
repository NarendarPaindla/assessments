# HTTP Protocol — The Language of the Internet

Before learning FastAPI routes, APIs, GET, POST, or databases, you must understand one thing:

> Client and Server cannot talk randomly.

They need a common language.

That language is **HTTP**.

---

# What is HTTP?

HTTP stands for:

```text
Hyper Text Transfer Protocol
```

Simple definition:

> HTTP is a set of rules that allows clients and servers to communicate with each other.

Think of HTTP as:

```text
English -> Human Communication

HTTP -> Computer Communication
```

Without HTTP:

```text
Browser ❌ Server
Mobile App ❌ Server
React App ❌ FastAPI
```

No communication would happen.

---

# Real Life Example

Imagine you go to a restaurant.

You cannot simply stare at the waiter and expect food.

You must communicate.

Example:

```text
Customer:
One Chicken Biryani please

Waiter:
Okay
```

Communication happened.

Similarly:

```text
Browser:
Give me students data

Server:
Here is the data
```

HTTP defines how this conversation should happen.

---

# Why HTTP Was Created?

Suppose a browser sends:

```text
Give me data
```

Server may ask:

```text
Which data?
```

Different companies could invent different formats.

Chaos!

So HTTP created standard rules.

Now every browser and server understand each other.

---

# Real Industry Example

When you open:

```text
https://www.youtube.com
```

Your browser sends HTTP requests.

YouTube server sends HTTP responses.

This happens thousands of times.

Every:

* Video
* Thumbnail
* Comment
* Like count
* User profile

comes through HTTP.

---

# How HTTP Works

```text
Client
   |
HTTP Request
   |
   V
Server
   |
HTTP Response
   |
   V
Client
```

This is the foundation of every web application.

---

# Example Using FastAPI

Imagine FastAPI server:

```python
from fastapi import FastAPI

app = FastAPI()

@app.get("/")
def home():
    return {"message":"Welcome"}
```

Browser requests:

```http
GET /
```

Server responds:

```json
{
  "message":"Welcome"
}
```

This entire communication uses HTTP.

---

# What Happens When You Open Google?

You type:

```text
google.com
```

Behind the scenes:

---

### Step 1

Browser sends request

```http
GET /
```

---

### Step 2

Google server receives request

---

### Step 3

Server processes request

---

### Step 4

Server sends HTML

```html
<html>
<body>
Google Homepage
</body>
</html>
```

---

### Step 5

Browser renders page

You see Google.

---

# HTTP is Stateless

One of the most important interview concepts.

---

## What is Stateless?

Server does NOT remember previous requests.

Example:

Request 1

```http
GET /profile
```

Server responds.

Request finished.

---

Later:

Request 2

```http
GET /orders
```

Server treats it like a completely new request.

It does not automatically remember you.

---

# Real Life Example

Imagine a bank token system.

You visit counter.

Transaction complete.

You leave.

After 1 hour you return.

Employee asks again:

```text
Token Number?
```

Why?

Because interaction ended.

HTTP behaves similarly.

---

# Then How Does Login Work?

Good question.

If HTTP forgets everything:

```text
How does Instagram remember me?
```

Using:

* Sessions
* Cookies
* JWT Tokens

We will learn these later in FastAPI Authentication.

---

# Anatomy of an HTTP Request

A request has multiple parts.

```http
GET /students HTTP/1.1
Host: localhost:8000
```

---

Let's break it.

### GET

Method

---

### /students

Path

---

### HTTP/1.1

Protocol Version

---

### Host

Server Address

---

# HTTP Request Structure

```text
Request Line
Headers
Body (optional)
```

Visual:

```text
GET /students HTTP/1.1
Host: localhost:8000

Request Body
```

---

# HTTP Response Structure

Server sends:

```text
Status Line
Headers
Body
```

Example:

```http
HTTP/1.1 200 OK
Content-Type: application/json

{
  "students":[]
}
```

---

# Most Important Part: HTTP Methods

Methods tell server what action we want.

Think of methods as verbs.

---

# GET

Meaning:

```text
Give me data
```

Examples:

```http
GET /students
GET /products
GET /users
```

Used for:

* Fetching records
* Reading information

---

# Real Example

Instagram Home Feed

```http
GET /posts
```

Server returns posts.

---

# POST

Meaning:

```text
Create New Data
```

Example:

```http
POST /students
```

Data:

```json
{
  "name":"Ravi"
}
```

Server creates student.

---

# Real Example

Instagram New Post

```http
POST /posts
```

Creates new post.

---

# PUT

Meaning:

```text
Update Entire Record
```

Example:

```http
PUT /students/1
```

Update student completely.

---

# PATCH

Meaning:

```text
Update Partial Record
```

Example:

```http
PATCH /students/1
```

Only update email.

---

# DELETE

Meaning:

```text
Remove Data
```

Example:

```http
DELETE /students/1
```

Deletes student.

---

# CRUD Mapping

Every database application follows CRUD.

```text
Create
Read
Update
Delete
```

HTTP Mapping:

| CRUD   | HTTP Method |
| ------ | ----------- |
| Create | POST        |
| Read   | GET         |
| Update | PUT/PATCH   |
| Delete | DELETE      |

Memorize this table.

Interviewers love this question.

---

# HTTP Status Codes

Server responds with numbers.

These numbers tell what happened.

---

# 200 OK

Meaning:

```text
Everything successful
```

Example:

```http
200 OK
```

---

# 201 Created

Meaning:

```text
New resource created
```

Example:

Student created.

```http
201 Created
```

---

# 400 Bad Request

Meaning:

```text
Client sent invalid data
```

Example:

```json
{
  "age":-10
}
```

Validation fails.

---

# 401 Unauthorized

Meaning:

```text
Login Required
```

Example:

Accessing profile without login.

---

# 403 Forbidden

Meaning:

```text
Permission denied
```

Example:

Normal user trying to delete all users.

---

# 404 Not Found

Most famous status code.

Example:

```http
GET /abcxyz
```

Route doesn't exist.

Response:

```http
404 Not Found
```

---

# 500 Internal Server Error

Meaning:

Something broke on server.

Example:

Database crashed.

Bug in code.

---

# Status Code Categories

```text
1xx -> Information

2xx -> Success

3xx -> Redirect

4xx -> Client Error

5xx -> Server Error
```

Interview favorite question.

---

# Real Industry Example

Student Management System

Frontend requests:

```http
GET /students
```

---

Server:

```text
Fetch from Database
```

---

Response:

```http
200 OK
```

```json
[
 {
   "id":1,
   "name":"Ravi"
 }
]
```

---

# What FastAPI Does Internally

When FastAPI receives:

```http
GET /students
```

It:

```text
Receive Request
      ↓
Match Route
      ↓
Execute Function
      ↓
Generate JSON
      ↓
Send Response
```

Example:

```python
from fastapi import FastAPI

app = FastAPI()

@app.get("/students")
def get_students():
    return [
        {"id":1,"name":"Ravi"}
    ]
```

Client sends:

```http
GET /students
```

Server responds:

```json
[
 {
   "id":1,
   "name":"Ravi"
 }
]
```

---

# Hands-On Exercise

Imagine you are building a College Portal.

### Fetch Students

```http
GET /students
```

Question:

Create or Read?

Answer:

```text
Read
```

---

### Add Student

```http
POST /students
```

Question:

Create or Read?

Answer:

```text
Create
```

---

### Update Student

```http
PUT /students/1
```

Answer:

```text
Update
```

---

### Delete Student

```http
DELETE /students/1
```

Answer:

```text
Delete
```

---

# Industry-Level Understanding

Every modern application:

* React Frontend
* Angular Frontend
* Vue Frontend
* Flutter Mobile App
* Android App
* iOS App

communicates with backend using HTTP.

Example:

```text
React
   |
HTTP
   |
FastAPI
   |
Database
```

Without HTTP:

```text
Frontend ❌ Backend
```

No communication is possible.

---

# Interview Questions

### What is HTTP?

HTTP (Hyper Text Transfer Protocol) is a communication protocol used for exchanging data between clients and servers.

---

### Why is HTTP called Stateless?

Because each request is independent and the server does not automatically remember previous requests.

---

### Which HTTP method is used to create data?

```text
POST
```

---

### Which method fetches data?

```text
GET
```

---

### Which status code indicates success?

```text
200 OK
```

---

### Which status code indicates resource not found?

```text
404 Not Found
```

---

# Golden Rule

Whenever a FastAPI application runs, this cycle repeats millions of times:

```text
Client
   ↓
HTTP Request
   ↓
FastAPI Server
   ↓
Business Logic
   ↓
Database
   ↓
HTTP Response
   ↓
Client
```

If you master HTTP, learning FastAPI routes, request bodies, query parameters, authentication, JWT, and REST APIs becomes much easier.
